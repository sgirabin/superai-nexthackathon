{
  "buildCommand": "pnpm --filter @goaround/agent-core build && pnpm --filter @goaround/web build",
  "installCommand": "pnpm install --frozen-lockfile=false",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs"
}
